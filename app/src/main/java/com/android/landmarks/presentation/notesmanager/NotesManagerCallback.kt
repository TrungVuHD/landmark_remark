package com.android.landmarks.presentation.notesmanager

/**
 * To make an interaction between [NotesManagerActivity] & its children
 * */
interface NotesManagerCallback {
    // at the moment, we just use only 1 simple fragment to handle our requirements
    // for the future , if we plan to add another fragment,
    // we can use this to interact between LandmarksManagerActivity and its children
}
